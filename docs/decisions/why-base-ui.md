# Why Base UI

## Decision

We chose **Base UI** (MUI Base) over **Radix UI** as our component library foundation.

## Rationale

### Why Base UI

#### Active Maintenance

Base UI is actively maintained by the MUI team with regular updates, bug fixes, and new features. Radix UI appears to have slowed down significantly, with less frequent updates and community activity.

